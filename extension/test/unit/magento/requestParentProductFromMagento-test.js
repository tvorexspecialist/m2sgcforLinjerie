const step = require('../../../magento/requestParentProductFromMagento')
const assert = require('assert')

describe('requestParentProductFromMagento', () => {
  it('should request a parent product from magento', (done) => {
    done()
  })
})
