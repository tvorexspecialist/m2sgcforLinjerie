const assert = require('assert')
const step = require('../../../../magento/helpers/setFirstElementAsOutputKey')

describe('setFirstElementAsOutputKey', () => {
  it('should set the first element as output key', (done) => {
    done()
  })
})
