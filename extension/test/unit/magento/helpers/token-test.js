const assert = require('assert')
const step = require('../../../../magento/helpers/token')

describe('token', () => {
  it('should get token from magento', (done) => {
    done()
  })

  it('should get token from store', (done) => {
    done()
  })
})
