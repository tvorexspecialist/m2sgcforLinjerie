const assert = require('assert')
const step = require('../../../../magento/helpers/exportAsObject')

describe('exportAsObject', () => {
  it('should export an element in an complete object', (done) => {
    done()
  })
})
